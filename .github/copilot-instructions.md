# Copilot Instructions

Follow `AGENTS.md` for project rules. The short version: this is the primary Wizard Make Potion repo with one Fastify API, one Vite React app, Supabase Postgres migrations, centralized theme tokens, no emoji in UI, development/production environment support through `.env.local`, and the archive folder as reference only.
